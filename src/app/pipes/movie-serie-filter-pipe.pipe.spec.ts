import { MovieSerieFilterPipePipe } from './movie-serie-filter-pipe.pipe';

describe('MovieSerieFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MovieSerieFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
